/*import cron from 'node-cron';
import { sincronizarDados, associarGrupos } from '../services/syncService';

// Agendar sincronização e associação a cada hora
cron.schedule('0 * * * *', async () => {
  console.log('Iniciando sincronização de dados...');
  await sincronizarDados();
  await associarGrupos();
  console.log('Sincronização e associação concluídas.');
});*/
